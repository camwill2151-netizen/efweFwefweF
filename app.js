const tbody=document.querySelector("#coinsTable tbody"),statusEl=document.getElementById("status"),refreshBtn=document.getElementById("refreshBtn");async function fetchCoins(){statusEl.textContent="Loading...";refreshBtn.disabled=true;try{const res=await fetch("https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page=15&page=1&sparkline=false");if(!res.ok)throw new Error(`HTTP ${res.status}`);const data=await res.json();tbody.innerHTML="";for(const coin of data){const tr=document.createElement("tr"),pct=coin.price_change_percentage_24h??0;tr.innerHTML=`<td>${coin.name}</td><td>${coin.symbol.toUpperCase()}</td><td>$${Number(coin.current_price).toLocaleString()}</td><td class="${pct>=0?"pos":"neg"}">${pct.toFixed(2)}%</td><td>$${Number(coin.market_cap).toLocaleString()}</td>`;tbody.appendChild(tr)}statusEl.textContent=`Updated: ${new Date().toLocaleTimeString()}`}catch(e){console.error(e);statusEl.textContent="Failed to load market data"}finally{refreshBtn.disabled=false}}refreshBtn.addEventListener("click",fetchCoins);fetchCoins();
